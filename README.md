# Find My Mistrian — source repository

The installable MOMI/MMAPI mod is in [`find_my_mistrian/`](find_my_mistrian/README.md).

Keep that directory intact and place it directly inside the Fields of Mistria `mods` directory.
